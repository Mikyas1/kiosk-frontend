import { store as auth } from "./auth";
import { store as dashboard } from "./dashboard";
import { store as lang } from "./lang";
import { store as home } from "./home";

export default { auth, dashboard, lang, home };
